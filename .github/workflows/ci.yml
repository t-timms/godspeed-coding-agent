name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --all-extras
      - run: uv run ruff check .
      - run: uv run ruff format --check .

  type-check:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --all-extras
      - run: uv run mypy src/ --ignore-missing-imports --disable-error-code arg-type --disable-error-code assignment --disable-error-code misc --disable-error-code list-item --disable-error-code type-abstract --disable-error-code type-arg --disable-error-code unused-ignore --disable-error-code override --disable-error-code call-arg --disable-error-code var-annotated --disable-error-code attr-defined --disable-error-code no-any-return --disable-error-code union-attr --disable-error-code return-value --disable-error-code no-redef

  security:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --all-extras
      # Chromadb CVE-2026-45830 / CVE-2026-45831 / CVE-2026-45833
      # (PYSEC-2026-311 covers the code-injection issue): no patched release
      # exists (all <= 1.5.9 affected). Tolerable here: chromadb is embedded-only
      # (local PersistentClient behind the optional [index] extra); the vulnerable
      # server API is never run. Drop these ignores when upstream ships a fix.
      - run: uv run pip-audit --ignore-vuln CVE-2026-28684 --ignore-vuln CVE-2026-3219 --ignore-vuln GHSA-mv93-w799-cj2w --ignore-vuln PYSEC-2026-311 --ignore-vuln CVE-2026-45830 --ignore-vuln CVE-2026-45831 --ignore-vuln CVE-2026-45833
      - run: uv run bandit -r src/ -ll -s B310,B104,B602,B404,B603,B607,B608,B101,B303,B301,B311,B615

  test:
    runs-on: ubuntu-latest
    needs: [type-check, security]
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
      - run: uv sync --all-extras
      - run: uv run pytest --cov --cov-report=term-missing -q
